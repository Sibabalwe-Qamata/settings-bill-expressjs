<html>
  <head>
    <meta charset="utf-8">
    <title></title>
    
  </head>
  <body>
        <div class="container">
            <div class="row">
                <div class="twelve columns box">
                      <h2>
                          Phone Bill Records:
                      </h2>
            
                <table >
                    {{#each billArray}}
                    <tr> 
                        <td>type:</td><td>{{Type}}</td>
                        <td>price:</td><td>{{Cost}}</td>
                        <td>date:</td><td>{{updatedDate}}</td>
                    </tr>
                      {{/each}} 
                </table>
              
                </div> 
            </div>
        </div>
  </body>
</html>
